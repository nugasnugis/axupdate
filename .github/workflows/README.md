# GitHub Actions Workflows

## CI
- `.github/workflows/ci.yml` installs dependencies and validates `axupdate.py` syntax.

## VNC Simulation
- `.github/workflows/vnc.yml` sets up a headless X11 display and starts `axupdate.py` for browser-based VNC testing.
